<%= render 'shared/search_bar' %>
<%#= render 'shared/search_filter' %>

<div class="floating-nav">
<ul class="nav nav-pills">
  <li class="nav-item active">
    <a class="nav-link" href="#1" data-toggle="tab">List</a>
  </li>
  <li class="nav-item">
    <a class="nav-link js-refresh-size" href="#2" data-toggle="tab">Map</a>
  </li>
</ul>
</div>

<div class="tab-content">
  <div role="tabpanel" class="list-cards tab-pane active" id="1">
    <div style="margin-top: 20px;">
      <div class="index-content text-center ">
        <!-- <h3>Audio tours near you</h3> -->
        <hr>
        <hr>
        <hr>
        <div class="category-tags">
  <% Tour::Categories.each do |category| %>
  <%= link_to category.upcase, tours_path(query: category), class: "btn-ghost" %>
  <%end %>
</div>

        <%= render 'shared/card' %>
      </div>
    </div>
  </div>

  <div role="tabpanel" class="map-card tab-pane active" role="tabpanel" id="2">
      <%= render 'shared/map' %>
  </div>

</div>
